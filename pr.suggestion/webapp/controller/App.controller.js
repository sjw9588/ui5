sap.ui.define([
  "sap/ui/core/mvc/Controller"
], function (Controller) {
  "use strict";

  return Controller.extend("sync.cc.mm.pr.suggestion.controller.App", {
    // 현재 컨트롤러에는 별도의 로직이 필요하지 않습니다.
  });
});